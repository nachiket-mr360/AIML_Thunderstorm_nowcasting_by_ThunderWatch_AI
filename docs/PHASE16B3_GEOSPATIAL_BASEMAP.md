# Phase 16B.3 — MapLibre geospatial basemap

MapTiler vector tiles via MapLibre GL JS. Key from `MAPTILER_API_KEY` only (see `.env.example`).

If the key is missing, tiles fail, or WebGL is unavailable, the Phase 16B.2 extruded India view is labeled **Fallback geographic view**.

Radar / satellite / lightning remain **NOT CONNECTED**.
